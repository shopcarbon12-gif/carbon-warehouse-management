node_modules
.next
.git
# Flutter app — not part of the Next.js production image; keeps Coolify context small/fast.
mobile
.tools
*.md
.env*.local
# Local/machine env must not be baked in; Coolify injects DATABASE_URL at runtime.
# Without this, next build inside Docker can hit localhost Postgres and fail or hang.
.env
.env.*
!.env.example
e2e
playwright-report
test-results
.cursor
coverage
